import React from 'react'

const Skill = ({ children }) => (
  <span style={{
    background: '#333',
    // color: '#EED4CD',
    padding: '0px 8px',
    marginRight: 10,
    marginBottom: 10,
    display: 'inline-block',
    fontSize: 13,
  }}>{children}</span>
)

export default Skill
